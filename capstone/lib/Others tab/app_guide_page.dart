import 'package:flutter/material.dart';

class AppGuidePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('App Guide'),
      ),
      body: Center(
        child: Text('App Guide Content Here'),
      ),
    );
  }
}
